git add src/soft/
git status
# Verificar que APENAS src/soft/** está sendo adicionado
# Se aparecer qualquer outro arquivo: parar e investigar

git commit -m "chore: cria estrutura de pastas src/soft — fase 1 etapa 1

- 12 pastas criadas em src/soft/
- nenhum arquivo existente alterado
- nenhuma rota registrada ainda
- nenhum model criado ainda
- isolamento total do sistema atual confirmado"

git push origin main